<!DOCTYPE html>
<html lang="fa" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> | Folder2Txt</title>
    <!-- Font Awesome -->
    <link rel="stylesheet" href="/assets/css/font-awesome-all.min.css">
    <!-- فونت وزیر (اختیاری) -->
    <link rel="stylesheet" href="/assets/css/Vazirmatn-font-face.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Vazirmatn', 'Segoe UI', Tahoma, system-ui, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 30px;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.5);
            max-width: 800px;
            width: 100%;
            padding: 40px;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 2.5rem;
            color: #1e293b;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
        }

        .header h1 i {
            color: #667eea;
        }

        .header p {
            color: #475569;
            font-size: 1.1rem;
            line-height: 1.6;
        }

        .tabs {
            display: flex;
            gap: 10px;
            margin-bottom: 25px;
            border-bottom: 2px solid #e2e8f0;
            padding-bottom: 10px;
        }

        .tab-btn {
            padding: 10px 20px;
            background: none;
            border: none;
            border-radius: 30px;
            font-size: 1rem;
            font-weight: 600;
            color: #64748b;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .tab-btn i {
            font-size: 1.2rem;
        }

        .tab-btn.active {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.3);
        }

        .tab-content {
            display: none;
        }

        .tab-content.active {
            display: block;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: #334155;
            font-size: 0.95rem;
        }

        .input-wrapper {
            position: relative;
            display: flex;
            align-items: center;
        }

        .input-wrapper i {
            position: absolute;
            right: 15px;
            color: #94a3b8;
            font-size: 1.2rem;
            pointer-events: none;
        }

        input[type="text"] {
            width: 100%;
            padding: 15px 50px 15px 20px;
            border: 2px solid #e2e8f0;
            border-radius: 50px;
            font-size: 1rem;
            font-family: 'Fira Code', monospace;
            direction: ltr;
            text-align: left;
            transition: all 0.3s ease;
            background: white;
        }

        input[type="text"]:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.2);
        }

        .drop-zone {
            border: 3px dashed #cbd5e1;
            border-radius: 20px;
            padding: 40px 20px;
            text-align: center;
            background: #f1f5f9;
            transition: all 0.3s ease;
            cursor: pointer;
            margin-bottom: 20px;
        }

        .drop-zone.drag-over {
            border-color: #667eea;
            background: #e0e7ff;
            transform: scale(1.02);
        }

        .drop-zone i {
            font-size: 3rem;
            color: #94a3b8;
            margin-bottom: 10px;
        }

        .drop-zone p {
            color: #475569;
        }

        .browse-btn {
            background: #667eea;
            color: white;
            border: none;
            padding: 8px 20px;
            border-radius: 30px;
            margin-top: 10px;
            cursor: pointer;
            font-size: 0.95rem;
            transition: 0.2s;
        }

        .browse-btn:hover {
            background: #5a67d8;
        }

        .file-types {
            margin: 25px 0;
        }

        .file-type-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(120px, 1fr));
            gap: 10px;
            margin-top: 15px;
            max-height: 200px;
            overflow-y: auto;
            padding: 5px;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            background: #f8fafc;
        }

        .file-type-item {
            display: flex;
            align-items: center;
            padding: 6px 10px;
            background: white;
            border-radius: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.05);
            transition: all 0.2s ease;
        }

        .file-type-item:hover {
            background: #edf2f7;
            transform: translateY(-1px);
        }

        .file-type-item input {
            margin-left: 6px;
        }

        .file-type-item label {
            font-size: 0.9rem;
            font-weight: 500;
            color: #334155;
            cursor: pointer;
            flex: 1;
        }

        .file-count {
            background: #667eea;
            color: white;
            padding: 2px 6px;
            border-radius: 12px;
            font-size: 0.7rem;
            margin-right: 5px;
        }

        .file-type-controls {
            display: flex;
            gap: 10px;
            margin-bottom: 10px;
        }

        .file-type-controls button {
            padding: 6px 12px;
            background: #e2e8f0;
            border: none;
            border-radius: 20px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: 0.2s;
        }

        .file-type-controls button:hover {
            background: #cbd5e1;
        }

        .options-panel {
            background: #f8fafc;
            padding: 20px;
            border-radius: 16px;
            margin: 25px 0;
        }

        .option-row {
            margin-bottom: 15px;
        }

        .option-row label {
            display: block;
            font-weight: 600;
            margin-bottom: 5px;
            color: #334155;
        }

        .option-row input[type="text"] {
            width: 100%;
            padding: 10px 15px;
            border: 2px solid #e2e8f0;
            border-radius: 30px;
            font-size: 0.95rem;
            transition: 0.3s;
        }

        .option-row input[type="text"]:focus {
            border-color: #667eea;
            outline: none;
        }

        .checkbox-option {
            display: flex;
            align-items: center;
            gap: 8px;
            margin-bottom: 10px;
        }

        .checkbox-option input {
            width: 18px;
            height: 18px;
        }

        .status-message {
            padding: 12px 20px;
            border-radius: 30px;
            margin: 15px 0;
            display: none;
        }

        .status-message.success {
            background: #d1fae5;
            color: #065f46;
            border: 1px solid #a7f3d0;
        }

        .status-message.error {
            background: #fee2e2;
            color: #991b1b;
            border: 1px solid #fecaca;
        }

        .status-message.processing {
            background: #fff3cd;
            color: #856404;
            border: 1px solid #ffe7a0;
        }

        .progress-bar {
            width: 100%;
            height: 8px;
            background: #e2e8f0;
            border-radius: 4px;
            overflow: hidden;
            margin: 15px 0;
            display: none;
        }

        .progress-fill {
            height: 100%;
            background: linear-gradient(90deg, #667eea, #764ba2);
            width: 0%;
            transition: width 0.3s ease;
        }

        .btn-primary {
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 40px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
        }

        .btn-primary:hover:not(:disabled) {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.4);
        }

        .btn-primary:disabled {
            opacity: 0.6;
            cursor: not-allowed;
        }

        .btn-secondary {
            width: 100%;
            padding: 14px;
            background: #10b981;
            color: white;
            border: none;
            border-radius: 40px;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            margin-top: 10px;
            display: none;
        }

        .btn-secondary:hover {
            background: #059669;
        }

        .file-list {
            max-height: 250px;
            overflow-y: auto;
            background: white;
            border: 1px solid #e2e8f0;
            border-radius: 12px;
            padding: 15px;
            margin-top: 15px;
            font-size: 0.85rem;
        }

        .file-list-item {
            padding: 5px 0;
            border-bottom: 1px solid #edf2f7;
            color: #1e293b;
        }

        .footer-note {
            text-align: center;
            margin-top: 30px;
            color: #64748b;
            font-size: 0.9rem;
            border-top: 1px solid #e2e8f0;
            padding-top: 20px;
        }

        .footer-note i {
            color: #ef4444;
        }

        @media (max-width: 640px) {
            .tabs {
                flex-direction: column;
            }
        }
    </style>
</head>
<body>
    <div class="card">
        <div class="header">
            <h1><i class="fas fa-folder-open"></i> Folder2Txt</h1>
            <p>پوشه‌ی خود را به یک فایل متنی یکپارچه تبدیل کنید.</p>
        </div>

        <!-- محتوای اصلی از index.ejs در اینجا قرار می‌گیرد -->
        <%- body %>

        <div class="footer-note">
            <i class="fas fa-heart"></i> 
            <span>پوشه‌های node_modules، .git و فایل‌های باینری نادیده گرفته می‌شوند.</span>
        </div>
    </div>

    <script src="/assets/js/main.js"></script>
</body>
</html>